import { Blocks } from '../../blocks';
import { Pieces } from '../pieces';

export class Angle extends Pieces{

    public possibleMoves(blockArray: Blocks):Blocks[]{
        let newBlocksArray = new Array<Blocks>();

        return newBlocksArray
    }
}

